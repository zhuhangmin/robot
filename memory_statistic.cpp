#include "stdafx.h"
#include "memory_statistic.h"
#include "user_manager.h"
void MemoryStatistic::SnapShot() {
    auto total_game_memory = 0;

    // UserManager 
    //
    /*auto user_map = UserMgr.GetAllUsers();
    for (auto kv : user_map) {
    auto userid = kv.first;
    auto user = kv.second;
    auto size = sizeof(*user);
    total_game_memory += size;

    }
    UWL_INF("total_game_memory = %d BYTE", total_game_memory);*/

    // RoomManager

    // GameInfoManager

    // DepositManager

    // RobotManager

    // HallManager
}
